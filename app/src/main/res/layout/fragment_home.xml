<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAFAFA"
    tools:context=".ui.home.HomeFragment">

    <!-- 상단: 로고/앱 이름 -->
    <ImageView
        android:id="@+id/homelogo"
        android:src="@drawable/logo"
        android:layout_width="60dp"
        android:layout_height="30dp"
        android:layout_margin="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>


    <!-- 이번 달 총 지출 카드 (+ 우측 > 표시) -->
    <androidx.cardview.widget.CardView
        android:id="@+id/card_month_total"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="8dp"
        app:cardUseCompatPadding="true"
        app:cardElevation="2dp"
        app:layout_constraintTop_toBottomOf="@id/homelogo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp">

            <TextView
                android:id="@+id/tv_month_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/label_this_month_total"
                android:textColor="@android:color/black"
                android:textSize="16sp"
                android:layout_alignParentStart="true"/>

            <!-- 우측 꺾쇠(>), 아이콘 리소스 없이 텍스트로 -->
            <TextView
                android:id="@+id/tv_chevron"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="&gt;"
                android:textSize="18sp"
                android:textStyle="bold"
                android:layout_alignParentEnd="true"
                android:layout_alignTop="@id/tv_month_title"/>

            <!-- 큰 금액 -->
            <TextView
                android:id="@+id/tv_total_expense_amount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="₩ 1,234,000"
                android:textStyle="bold"
                android:textSize="28sp"
                android:layout_below="@id/tv_month_title"
                android:layout_marginTop="8dp"/>

            <!-- 환율 기준 토글 (라디오 점) -->
            <LinearLayout
                android:id="@+id/layout_fx_basis"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_below="@id/tv_total_expense_amount"
                android:layout_marginTop="8dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/label_fx_basis"
                    android:textColor="@color/sub_text"/>

                <RadioGroup
                    android:id="@+id/rg_fx_basis"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:layout_marginStart="8dp">

                    <RadioButton
                        android:id="@+id/rb_today"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/fx_today"
                        android:button="@drawable/radio_dot_selector"
                        android:paddingEnd="8dp"/>

                    <RadioButton
                        android:id="@+id/rb_at_spend"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/fx_at_spend"
                        android:button="@drawable/radio_dot_selector"/>
                </RadioGroup>
            </LinearLayout>

        </RelativeLayout>
    </androidx.cardview.widget.CardView>

    <!-- 날짜 섹션 -->
    <TextView
        android:id="@+id/tv_section_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="2025. 11. 04"
        android:textSize="14sp"
        android:textStyle="bold"
        android:layout_marginStart="16dp"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/card_month_total"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- 최근 지출 목록 (리사이클러뷰) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_recent"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:paddingHorizontal="8dp"
        android:paddingTop="4dp"
        app:layout_constraintTop_toBottomOf="@id/tv_section_date"
        app:layout_constraintBottom_toTopOf="@id/box_notes"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:listitem="@layout/list_item_expense_home"/>

    <!-- 하단 메모 박스(1,2,3 항목) -->
    <androidx.cardview.widget.CardView
        android:id="@+id/box_notes"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="12dp"
        app:cardUseCompatPadding="true"
        app:cardElevation="1dp"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="1. 더 추가할 기능?"
                android:textSize="14sp"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="2. 최근 지출내역 3칸/5칸으로?"
                android:textSize="14sp"
                android:layout_marginTop="4dp"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="3. 환율 정보툴 작게?(travelWallet 참고)"
                android:textSize="14sp"
                android:layout_marginTop="4dp"/>
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- 퀵 지출 FAB + 라벨 -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_quick_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/quick_add"
        app:tint="@android:color/white"
        app:backgroundTint="@color/point_blue"
        app:srcCompat="@android:drawable/ic_input_add"
        app:maxImageSize="32dp"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="56dp"/>

    <TextView
        android:id="@+id/tv_quick_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/quick_add"
        android:textSize="12sp"
        app:layout_constraintTop_toBottomOf="@id/fab_quick_add"
        app:layout_constraintEnd_toEndOf="@id/fab_quick_add"
        android:layout_marginTop="6dp"/>

    <!-- 하단 BottomNavigation -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@android:color/white"
        app:itemIconTint="@color/bottom_nav_color_selector"
        app:itemTextColor="@color/bottom_nav_color_selector"
        app:menu="@menu/bottom_nav_menu"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:labelVisibilityMode="labeled"/>

</androidx.constraintlayout.widget.ConstraintLayout>
